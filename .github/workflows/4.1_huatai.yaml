name: Huatai Domain Auto Update

on:
  workflow_dispatch:

  schedule:
    - cron: "0 18 * * 5"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test workflow
        run: echo "Huatai workflow created successfully."

